import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.vv3.intento.3',
  appName: 'evaluacion-3-intento-3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
